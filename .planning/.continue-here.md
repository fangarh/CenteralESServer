---
context: delivery
phase: "3 - Docker Compose Delivery MVP"
status: docker_compose_real_smoke_complete
last_updated: 2026-06-03
last_commit: 24f212f
---

# Continue Here

## Current State

Последний сохранённый git checkpoint перед текущей документационной фиксацией:

```text
24f212f Fix source audit remediation tails
```

Последняя runtime-проверка:

- `docker compose -p centerales-real-smoke config --quiet` прошёл;
- `docker compose -p centerales-real-smoke build` собрал Web/Worker/Migrator images;
- `docker compose -p centerales-real-smoke up -d` поднял PostgreSQL -> migrator -> Web/Worker;
- `/health/live` и `/health/ready` вернули `200`;
- Worker стартовал с `PdfStampRecognition:Recognizer=Http`;
- Public upload ignored `test.pdf` -> Worker -> external `pdf2txt` `/recognize_json/` -> result polling прошёл с `completed`;
- compose stack остановлен через `docker compose -p centerales-real-smoke down`.

## Completed Work

- Phase 1 backend slice complete: Public API, PostgreSQL queue, Worker, pdf2txt boundary, result/status, admin read-only visibility, health/heartbeat.
- Public API key auth complete for public endpoints.
- Admin session cookie + CSRF baseline complete.
- Manual retry endpoint complete:
  - `POST /api/admin/jobs/{jobId}/retry`
  - admin session + `X-CSRF-Token`
  - creates queued attempt for current failed/blocked job
  - writes `manual_retry_job` audit event
- Temporary storage capacity guard complete:
  - `Storage:TemporarySoftLimitBytes`
  - `Storage:TemporaryHardLimitBytes`
  - `Storage:TemporaryMinimumFreeBytes`
  - `503 temporary_storage_full` on upload when exceeded
- Support report MVP complete:
  - `GET /api/admin/jobs/{jobId}/support-report`
  - admin session cookie required
  - read-only, no `X-CSRF-Token`
  - returns sanitized job/attempt diagnostics, passive processor/queue/worker context, result index reference, and related processing-job audit events
  - does not return input PDF, `temporaryFileKey`, raw secrets, or raw result payload
- Code audit/refactor checkpoint complete:
  - `src/Apps/CenteralES.Web/Program.cs` reduced from ~1200 lines to a small composition root
  - Web routes are split into `HealthEndpoints`, `PublicPdfEndpoints`, `AdminAuthEndpoints`, `AdminJobEndpoints`, `AdminProcessorEndpoints`
  - API contracts, auth helpers, and response mappings are in separate files
  - `ProcessingJobStatusMapper` centralizes status string conversion
  - manual retry action result is now typed records instead of enum plus nullable payload
- Docker Compose real processor smoke complete:
  - local ignored `.env` used `CENTERALES_PDF_RECOGNIZER=Http`;
  - endpoint was configured to the documented real `pdf2txt` `/recognize_json/`;
  - one-time Public API key was seeded into compose PostgreSQL without printing key/secret values;
  - public result returned contract `pdf2txt-recognize-json-v1`, not `fake-pdf2txt`.

## Required Reading

1. `AGENTS.md`
2. `docs/START_PROMPT_OFFICE.md`
3. `.planning/STATE.md`
4. `.planning/ROADMAP.md`
5. `.planning/REQUIREMENTS.md`
6. `ESServer/04 Админка/Админка MVP.md`
7. `ESServer/05 Данные и хранение/Файлы и Storage.md`
8. `ESServer/06 Эксплуатация/Безопасность и доступ.md`
9. `ESServer/06 Эксплуатация/Retry и дедупликация.md`

## Critical Rules

- Не выводить секреты из `db.env`.
- Не коммитить `db.env`, `.codex-local/`, `test.pdf`.
- Не использовать EF.
- Не смешивать Public API key auth и Admin session auth.
- State-changing Admin API требует `X-CSRF-Token`.
- Support report/audit не должен включать raw secrets, входной PDF или большие payload.

## Next Action

Рекомендуемый следующий шаг для MVP:

1. Сделать обычный `dotnet build` / `dotnet test` после документационного checkpoint-а, если нужен финальный release gate.
2. Commit текущих documentation/planning updates.
3. После этого логичный backlog: refactor `PostgresProcessingJobQueue` или завершение/полировка Admin MVP, потому что v1 delivery path теперь проверен через Compose с real `pdf2txt`.
